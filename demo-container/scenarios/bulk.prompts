# Bulk Scenario - Tests jira-bulk skill
# Bulk operations on multiple issues
---
prompt: |
  Create 3 low priority tasks in DEMO: Cleanup task 1, Cleanup task 2, Cleanup task 3
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [DEMO-, created, Task]
    must_not_contain: [error, failed]
  semantic: |
    Response should confirm creation of 3 tasks with low priority.
---
prompt: |
  Preview moving all issues with "Cleanup" in the summary to In Progress
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [preview, Cleanup]
    must_not_contain: [error, failed]
  semantic: |
    Response should show what would happen without making changes (dry-run).
---
prompt: |
  Move all issues containing "Cleanup" in DEMO to In Progress
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [In Progress]
    must_not_contain: [error, failed]
  semantic: |
    Response should confirm bulk transition of matching issues.
---
prompt: |
  Assign all my open tasks in DEMO to Jane Manager
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [assigned, Jane]
    must_not_contain: [error, failed]
  semantic: |
    Response should confirm bulk assignment to Jane Manager.
---
prompt: |
  Show me what would happen if I changed all low priority tasks in DEMO to medium
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [preview, priority]
    must_not_contain: [error, failed]
  semantic: |
    Response should preview the bulk priority change without executing.
---
prompt: |
  Change all low priority tasks in DEMO to medium priority
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [priority, Medium]
    must_not_contain: [error, failed]
  semantic: |
    Response should confirm bulk priority change was applied.
---
prompt: |
  Preview deleting all issues in DEMO with "Cleanup" in the summary
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [preview, delete, Cleanup]
    must_not_contain: [error, failed]
  semantic: |
    Response should show which issues would be deleted (dry-run).
---
prompt: |
  Delete all issues in DEMO with "Cleanup" in the summary
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [deleted]
    must_not_contain: [error, failed]
  semantic: |
    Response should confirm bulk deletion of matching issues.
---
prompt: |
  Show me issues matching: project = DEMO AND status = 'To Do' AND type = Task
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [DEMO-]
    must_not_contain: [error, failed]
  semantic: |
    Response should list issues matching the JQL criteria.
---
