# Fields Scenario - Tests jira-fields skill
# Custom field management
---
prompt: |
  Show me all custom fields in JIRA
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [field]
    must_not_contain: [error, failed]
  semantic: |
    Response should list custom fields with their names and IDs.
---
prompt: |
  Show me all Agile-related fields
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [field]
    must_not_contain: [error, failed]
  semantic: |
    Response should list Agile fields like Story Points, Sprint, etc.
---
prompt: |
  Find fields containing story in the name
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [Story]
    must_not_contain: [error, failed]
  semantic: |
    Response should list fields with "story" in the name.
---
prompt: |
  What fields are available for project DEMO?
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [field]
    must_not_contain: [error, failed]
  semantic: |
    Response should list fields configured for the DEMO project.
---
prompt: |
  What fields are available for Stories in DEMO?
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [Story, field]
    must_not_contain: [error, failed]
  semantic: |
    Response should list fields available for the Story issue type.
---
prompt: |
  Check if Agile fields are configured for project DEMO
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [Agile]
    must_not_contain: [error, failed]
  semantic: |
    Response should indicate Agile field configuration status.
---
prompt: |
  Is DEMO a team-managed or company-managed project?
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [DEMO]
    must_not_contain: [error, failed]
  semantic: |
    Response should identify the project type (team-managed or company-managed).
---
prompt: |
  Preview configuring Agile fields for DEMO
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [preview, Agile]
    must_not_contain: [error, failed]
  semantic: |
    Response should show what would be configured (dry-run).
---
prompt: |
  What is the field ID for Story Points?
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [Story Points]
    must_not_contain: [error, failed]
  semantic: |
    Response should show the field ID for Story Points custom field.
---
prompt: |
  Show me all fields including system fields
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [field]
    must_not_contain: [error, failed]
  semantic: |
    Response should list both system and custom fields.
---
