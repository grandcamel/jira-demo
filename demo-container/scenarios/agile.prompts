# Agile Scenario - Tests jira-agile skill
# Epic, sprint, and backlog management
---
prompt: |
  Show me the backlog for project DEMO
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [DEMO-]
    must_not_contain: [error, failed, "I don't have access"]
  semantic: |
    Response should show the project backlog with issues,
    their priorities, and status.
---
prompt: |
  What's in the current sprint for DEMO?
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [sprint]
    must_not_contain: [error, failed]
  semantic: |
    Response should list issues in the active sprint,
    or indicate if no sprint is active.
---
prompt: |
  What's the sprint progress for DEMO?
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [sprint]
    must_not_contain: [error, failed]
  semantic: |
    Response should show sprint progress including completed,
    in progress, and remaining work.
---
prompt: |
  Show me all epics in DEMO
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [DEMO-84, Epic]
    must_not_contain: [error, failed]
  semantic: |
    Response should list all epics in the project.
    Should include DEMO-84 (Product Launch).
---
prompt: |
  What's the progress on epic DEMO-84?
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [DEMO-84]
    must_not_contain: [error, "not found"]
  semantic: |
    Response should show epic progress including linked issues
    and completion percentage.
---
prompt: |
  What is Jane Manager working on in the current sprint?
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [Jane]
    must_not_contain: [error, failed]
  semantic: |
    Response should show issues assigned to Jane in the current sprint.
---
prompt: |
  Add DEMO-87 to the current sprint
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [DEMO-87, sprint]
    must_not_contain: [error, failed]
  semantic: |
    Response should confirm DEMO-87 was added to the active sprint.
---
prompt: |
  Set story points on DEMO-85 to 5
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [DEMO-85, 5]
    must_not_contain: [error, failed]
  semantic: |
    Response should confirm story points were set on DEMO-85.
---
prompt: |
  Create a story in DEMO: Implement API rate limiting, linked to epic DEMO-84, with 3 story points
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [DEMO-, created, Story]
    must_not_contain: [error, failed]
  semantic: |
    Response should confirm story creation with link to epic
    and story points set.
---
