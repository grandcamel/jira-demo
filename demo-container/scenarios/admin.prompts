# Admin Scenario - Tests jira-admin skill
# Project administration and permissions
---
prompt: |
  What permissions do I have on project DEMO?
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [permission]
    must_not_contain: [error, failed]
  semantic: |
    Response should list permissions the current user has on the project.
---
prompt: |
  What permissions am I missing on DEMO?
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [permission]
    must_not_contain: [error, failed]
  semantic: |
    Response should identify permissions not granted to the current user.
---
prompt: |
  Do I have permission to delete issues in DEMO?
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [delete]
    must_not_contain: [error, failed]
  semantic: |
    Response should indicate whether the user has DELETE_ISSUES permission.
---
prompt: |
  Show me the project roles and members for DEMO
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [role]
    must_not_contain: [error, failed]
  semantic: |
    Response should list project roles and their members.
---
prompt: |
  Who are the Administrators in project DEMO?
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [Administrator]
    must_not_contain: [error, failed]
  semantic: |
    Response should list users in the Administrators role.
---
prompt: |
  Add Jane Manager to the Developers role in DEMO
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [Jane, Developer]
    must_not_contain: [error, failed]
  semantic: |
    Response should confirm Jane was added to the Developers role.
---
prompt: |
  Preview removing Jane Manager from Developers role in DEMO
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [Jane, Developer, preview]
    must_not_contain: [error, failed]
  semantic: |
    Response should show what would happen without making changes (dry-run).
---
prompt: |
  Remove Jane Manager from the Developers role in DEMO
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [Jane, removed]
    must_not_contain: [error, failed]
  semantic: |
    Response should confirm Jane was removed from the Developers role.
---
prompt: |
  Search for users with "admin" in their name
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_not_contain: [error, failed]
  semantic: |
    Response should list users matching the search criteria.
---
prompt: |
  Show me all JIRA groups
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [group]
    must_not_contain: [error, failed]
  semantic: |
    Response should list available JIRA groups.
---
prompt: |
  Show me the configuration for project DEMO
expect:
  tools:
    must_call: [Skill]
    # must_not_call: [Bash]  # Relaxed - Claude may explore
  text:
    must_contain: [DEMO]
    must_not_contain: [error, failed]
  semantic: |
    Response should show project configuration including schemes and settings.
---
